import { FACTORY_BYTECODE, FACTORY_ABI } from '../contracts/bytecode';

// Export the deployment configuration
export const deployGameItems = {
    abi: FACTORY_ABI,
    bytecode: FACTORY_BYTECODE
}; 